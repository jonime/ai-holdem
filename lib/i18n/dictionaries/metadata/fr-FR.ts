import "server-only";

import type { MetadataDictionary } from "../../types";

const dictionary = {
  title: "AI Hold'em",
  description: "Une démo structurée de décisions de poker par l’IA TypeSafe.",
  logoAlt: "Logo AI Hold'em",
} satisfies MetadataDictionary;

export default dictionary;
