import "server-only";

const dictionary = {
  language: "Language",
  newGame: {
    yourName: "Your name",
    anonymous: "Anonymous",
    newGame: "New Game",
    createGameError: "Unable to create game",
  },
} as const;

export default dictionary;
